<?xml version="1.0" encoding="UTF-8" ?>
<!--MyBatis配置文件-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zxk.mapper.ArticleMapper">
    <resultMap id="articleMap" type="com.zxk.pojo.Article">
        <id column="id" property="id"/>
        <result column="author_id" property="authorId"/>
        <result column="comment_counts" property="commentCounts"/>
        <result column="create_date" property="createDate"/>
        <result column="summary" property="summary"/>
        <result column="title" property="title"/>
        <result column="view_counts" property="viewCounts"/>
        <result column="weight" property="weight"/>
        <result column="body_id" property="bodyId"/>
        <result column="category_id" property="categoryId"/>
    </resultMap>
    <!--List<Archives> listArchives();-->
    <select id="listArchives" resultType="com.zxk.vo.result.Archives">
        select FROM_UNIXTIME(create_date / 1000, '%Y') as year
             , FROM_UNIXTIME(create_date / 1000, '%m') as month
             , count(*)                                as count
        from article
        group by year, month
    </select>
    <!--Page<Article> page
            , Long categoryId
            , Long tagId
            , String year
            , String month-->
    <select id="listArticle" resultMap="articleMap">
        select id, author_id, comment_counts, create_date, summary, title, view_counts, weight, body_id, category_id
        from article
        <where>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="tagId != null">
                and id in (
                    select article_id from article_tag
                    where tag_id = #{tagId}
                )
            </if>
            <if test="year != null and year.length > 0 and month != null and month.length > 0">
                and FROM_UNIXTIME(create_date / 1000, '%Y') = #{year}
                and FROM_UNIXTIME(create_date / 1000, '%m') = #{month}
            </if>
        </where>
        order by weight,create_date desc
    </select>


</mapper>